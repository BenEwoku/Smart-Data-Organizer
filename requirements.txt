# Core framework
streamlit>=1.28.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# ============= WEB SCRAPING (ENHANCED) =============
# Basic scraping (you already have these ✓)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ADD THESE for enhanced scraping:
# HTML5 parser for better compatibility
html5lib>=1.1
uuid>=1.30

# Selenium for JavaScript-heavy sites (CRITICAL ADDITION)
selenium>=4.15.0
webdriver-manager>=4.0.1  # Auto-manages ChromeDriver

# User-Agent rotation for anti-bot measures
fake-useragent>=1.4.0

# Better HTTP handling
urllib3>=2.0.0
certifi>=2023.7.22  # For SSL certificate handling

# Retry logic for failed requests
tenacity>=8.2.3

# ============= FILE PARSING =============
# Excel support (you already have these ✓)
openpyxl>=3.1.0
xlrd>=2.0.1

# CSV handling improvements
chardet>=5.2.0  # Character encoding detection

# PDF support (you already have these ✓)
PyPDF2>=3.0.0
pdfplumber>=0.10.0

# ADD: Better PDF extraction
pdfminer.six>=20221105
tabula-py>=2.8.0  # Extract tables from PDFs

# Word document support (you already have ✓)
python-docx>=1.1.0

# ADD: Email parsing (for your MBOX feature)
python-magic>=0.4.27  # File type detection
python-magic-bin>=0.4.14; platform_system == "Windows"  # Windows support

# ============= DATA PROCESSING =============
# Date parsing improvements
python-dateutil>=2.8.2

# Missing value imputation (you already have scikit-learn ✓)
scipy>=1.10.0
scikit-learn>=1.3.0

# ADD: Better missing data handling
fancyimpute>=0.7.0  # Advanced imputation methods

# ============= AUTHENTICATION & PAYMENTS =============
# Google Sheets integration (you already have these ✓)
gspread>=5.12.0
google-auth>=2.23.0
google-auth-oauthlib>=1.0.0

# ADD: User authentication
bcrypt>=4.1.0  # Password hashing
PyJWT>=2.8.0  # JSON Web Tokens

# ADD: Payment processing (Stripe)
stripe>=7.0.0

# ============= DATA VALIDATION =============
# Schema validation
jsonschema>=4.19.0

# Email validation
email-validator>=2.1.0

# ============= VISUALIZATION =============
# Data visualization (you already have ✓)
plotly>=5.18.0

# ADD: Better charts
matplotlib>=3.8.0
seaborn>=0.13.0

# ============= TEXT PROCESSING =============
# Language detection (uncomment if needed)
langdetect>=1.0.9

# Better text processing (uncomment if needed)
regex>=2023.0

# Text cleaning
unidecode>=1.3.7  # Remove accents, normalize text

# ============= PERFORMANCE =============
# Caching and performance
cachetools>=5.3.2

# Parallel processing
joblib>=1.3.2

# ============= UTILITIES =============
# Progress bars
tqdm>=4.66.0

# YAML config support (for settings)
PyYAML>=6.0.1

# ============= DEVELOPMENT TOOLS =============
# Code quality (you already have these ✓)
black>=23.0.0
pytest>=7.4.0

# ADD: More dev tools
pylint>=3.0.0  # Code linting
mypy>=1.7.0  # Type checking

# Testing
pytest-cov>=4.1.0  # Test coverage
pytest-mock>=3.12.0  # Mocking for tests

# ============= ERROR TRACKING =============
# Production error monitoring
sentry-sdk>=1.38.0

# ============= COMPATIBILITY =============
# Ensure compatibility
typing-extensions>=4.8.0
packaging>=23.2

# ============= OPTIONAL BUT RECOMMENDED =============
# Rich terminal output (for CLI debugging)
rich>=13.7.0

# HTTP caching
requests-cache>=1.1.0

# JSON handling improvements
orjson>=3.9.10  # Faster JSON parsing

# ============= SECURITY =============
# Security scanning
safety>=2.3.5

# Environment variables
python-dotenv>=1.0.0

#pay pal
paypalrestsdk>=1.13.3
paypal-checkout-serversdk>=1.0.1

# ============= NOTES =============
# For Selenium to work, you also need:
# - Chrome browser installed
# - ChromeDriver (auto-installed by webdriver-manager)
# 
# For tabula-py to work, you need:
# - Java Runtime Environment (JRE) installed
#
# Platform-specific:
# - python-magic-bin is only for Windows
# - On Linux/Mac, install libmagic separately